{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Python Package",
            "type": "shell",
            "command": "cd ${workspaceFolder}; rm -rf build; venv/bin/python3 setup.py sdist bdist_wheel",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}